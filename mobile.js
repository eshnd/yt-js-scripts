setInterval(() => {
  document.querySelectorAll('.rich-section-content').forEach(div => div.remove());
  document.querySelectorAll('.pivot-shorts').forEach(div => div.remove());
  document.querySelectorAll('.reel-shelf-items').forEach(div => div.remove());
}, 100);

